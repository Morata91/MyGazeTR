---
test:

    load:
        begin_step: 10
        end_step: 10
        steps: 10

    data: 
        image: "../../datasets/processed/MPII_2d/Image"
        label: "../../datasets/processed/MPII_2d/Label"
        name: mpii
        header: True
        isFolder: True

    savename: "evaluation"

    device: 0

    reader: reader

# dropout = 0
# dim_feed = 512
